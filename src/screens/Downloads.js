import React from 'react';
import { Text, View } from 'react-native';
import { gStyle } from '../api/constants';

const Downloads = () => (
  <View style={gStyle.container}>
    <Text style={gStyle.heading}>Downloads</Text>
  </View>
);

export default Downloads;
